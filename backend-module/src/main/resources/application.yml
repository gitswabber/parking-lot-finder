spring:
  batch:
    job:
      enabled: false
  h2:
    console:
      enabled: true
  main:
    allow-bean-definition-overriding: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    display-request-duration: true